import { createContext } from 'react';

// FirebaseUserContext is a context object that is used to pass the user object, user data and loader state to the children components for the dashboard
export const FirebaseUserContext = createContext(null);
